Nearby Restaurants

%ul
  -businesses.each do |business|
    %li
      =link_to(image_tag(business.photo_url), business.url, :target => "_blank")
      =link_to(business.name, business.url, :target => "_blank")
      =business.distance
