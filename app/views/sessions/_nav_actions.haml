%ul.session
  -if signed_in?
    %li==Hi #{current_user.first_name}
    %li.action=link_to("Sign out", sign_out_path)
    %li.profile=link_to("My Cozily", edit_user_path(current_user))
  -else
    %li.action=link_to("Sign up", sign_up_path)
    %li.action=link_to("Sign in", sign_in_path)
