%ul#images{:'data-content-key' => 'images'}
  - apartment.photos.each do |image|
    = render "images/thumbnail", :image => image
  - for num in apartment.photos.count+1..6
    %li.empty
      %span.number= num
