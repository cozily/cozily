.account{:'data-content-key' => 'yield'}
  .article
    %h1 Almost done!
    %h2 Just tell us a little more to do.

    -semantic_form_for @user, :url => { :controller => "signup", :action => "account" } do |f|
      =render "users/inputs", :form => f

      -f.buttons do
        =f.commit_button "Sign Up & Start Browsing"

  =image_tag "characters/lady.png", :class => "lady"
